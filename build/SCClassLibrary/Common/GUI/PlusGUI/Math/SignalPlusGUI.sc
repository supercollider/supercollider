+ Signal {
	plot { arg name, bounds;
		this.asciiPlot;
		// open a window
		//name = name ? "signal";
		//bounds = bounds ? Rect.new(40,80,600,380);
		//SignalWindow.new(name, bounds, this);
	}
}

+ Wavetable {
	plot { arg name, bounds;
	}
}


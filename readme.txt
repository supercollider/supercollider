scons build targets:

debug_server: debug version of nova server
release_server: release version of nova server

server: build both debug and release versions of nova server

run_tests: run unit tests

To build a Debian package,
execute

scons PREFIX=/usr DEVELOPMENT=yes LID=yes

and then

scons debian 

The debian package will be put in the debian folder.

and can then be installed just like any other Debian package.

(scripts created by k.gebbert with minor modification by nescivi, 11/2007)